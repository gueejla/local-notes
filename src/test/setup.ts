// src/test/setup.ts
// Optional: Import jest-dom matchers for React Testing Library
// import '@testing-library/jest-dom';

// Optional: Global test utilities
// vi.mock('some-module', () => ({ ... }));

// Optional: Cleanup after each test
// afterEach(() => {
//   vi.clearAllMocks();
// });